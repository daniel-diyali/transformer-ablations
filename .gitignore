# Data artifacts — regenerated by data/prepare.py, never committed
data/
*.bin
tokenizer.json

# Run outputs — checkpoints and logs stay local.
# Only results.jsonl and figures/ are committed, so findings stay diffable.
runs/
*.pt
*.ckpt
wandb/

# Throwaway prototypes (see PLAN.md risk register)
spikes/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/
