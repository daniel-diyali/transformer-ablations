# transformer-ablations
A GPT-style decoder built from scratch in PyTorch, trained at ~14M params, with controlled ablations on layer-norm placement, positional encoding, and attention head count. Multi-seed, reproducible.
